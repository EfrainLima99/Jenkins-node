pipeline {
    agent any

    tools {nodejs "NodeJS"}

    stages {
        stage('install') {
            steps {
                bat 'npm install'
            }
        }
        stage('deploy'){
            steps{
                sh 'npm start'
            }
        }
    }
}