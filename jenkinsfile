pipeline {
    agent any

    tools {nodejs "NodeJS"}

    stages {
        stage('install') {
            steps {
                sh 'npm install'
            }
        }
        stage('deploy'){
            steps{
                sh 'npm start'
            }
        }
    }
}